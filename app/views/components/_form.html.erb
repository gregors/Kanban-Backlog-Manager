<%= form_for(@component) do |f| %>
  <% if @component.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@component.errors.count, "error") %> prohibited this post from being saved:</h2>
    <ul>
    <% @component.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
 
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>